      <div class="cadreNoir">
        <table class="sousTitre">
          <tr>
            <td><img class="micro" src="{{img_path}}{{image}}"></td>
            <td><h2>{{titre}}</h2></td>
          </tr>
        </table>
        <br>
        <div class="container-fluid">
          <div class="row">
        {% for score in lignes %}
            <div class="col-6">
              <h3>Niveau {{score.niveau}} : {{ (score.score*100/6)|number_format(0,'.',',') }}% </h3>
            </div>
        {% else %}<h3>Tu n'as pas encore joué dans ce niveau.</h3>
        {% endfor %}
          </div>
        </div>
      </div>